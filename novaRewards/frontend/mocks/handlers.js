import { http, HttpResponse } from 'msw';\nimport { createUser, createCampaign, createTransaction } from '../__tests__/factories/index.js';\n\n// MSW handlers using factories\nexport const handlers = [\n  http.get('/api/leaderboard', () => {\n    return HttpResponse.json({\n      leaderboard: Array.from({length: 10}, createUser),\n    });\n  }),\n\n  http.get('/api/campaigns', () => {\n    return HttpResponse.json({\n      campaigns: Array.from({length: 5}, createCampaign),\n    });\n  }),\n\n  http.post('/api/transactions', async ({ request }) => {\n    const body = await request.json();\n    return HttpResponse.json({\n      transaction: {\n        ...createTransaction(),\n        ...body,\n      },\n    });\n  }),\n\n  // More API mocks\n  http.get('/api/user/:id', ({ params }) => {\n    return HttpResponse.json(createUser());\n  }),\n];\n\n// Use in setupTests.js or test files
