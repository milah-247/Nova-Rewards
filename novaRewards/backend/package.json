{
  "name": "nova-rewards-backend",
  "version": "1.0.0",
  "private": true,
  "description": "NovaRewards backend API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:integration": "vitest run --project integration",
    "test:ci": "vitest run --coverage --reporter=verbose --reporter=junit --outputFile=test-results/junit.xml",
    "lint": "eslint .",
    "security": "vitest run tests/security",
    "migrate": "node ../database/migrate.js",
    "migrate:rollback": "node ../database/migrate.js --rollback",
    "db:migrate": "node ../database/migrate.js",
    "generate:openapi": "node -e \"const fs=require('fs'); const swagger=require('./swagger'); fs.mkdirSync('../..//docs', { recursive: true}); fs.writeFileSync('../../docs/openapi.json', JSON.stringify(swagger, null, 2)); console.log('docs/openapi.json generated');\"",
    "load:webhook": "k6 run -e BASE_URL=${BASE_URL:-http://localhost:3001} -e WEBHOOK_SECRET=${WEBHOOK_SECRET:-test_secret} --out json=test-results/load/webhook-actions-raw.json tests/load/scenarios/webhook-actions.js",
    "load:balance": "k6 run -e BASE_URL=${BASE_URL:-http://localhost:3001} -e USER_TOKEN=${USER_TOKEN} -e USER_ID_MIN=${USER_ID_MIN:-1} -e USER_ID_MAX=${USER_ID_MAX:-100} --out json=test-results/load/user-balance-raw.json tests/load/scenarios/user-balance.js",
    "load:campaigns": "k6 run -e BASE_URL=${BASE_URL:-http://localhost:3001} -e MERCHANT_API_KEYS=${MERCHANT_API_KEYS} --out json=test-results/load/campaigns-list-raw.json tests/load/scenarios/campaigns-list.js",
    "load:all": "k6 run -e BASE_URL=${BASE_URL:-http://localhost:3001} -e WEBHOOK_SECRET=${WEBHOOK_SECRET:-test_secret} -e USER_TOKEN=${USER_TOKEN} -e USER_ID_MIN=${USER_ID_MIN:-1} -e USER_ID_MAX=${USER_ID_MAX:-100} -e MERCHANT_API_KEYS=${MERCHANT_API_KEYS} --out json=test-results/load/full-suite-raw.json tests/load/full-suite.js",
    "load:seed": "node tests/load/seed.js"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1115.0",
    "@elastic/elasticsearch": "^9.5.0",
    "@prisma/client": "^7.9.1",
    "bcryptjs": "^3.0.3",
    "bullmq": "^6.2.0",
    "class-validator": "^0.15.1",
    "compression": "1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "eslint": "^10.8.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "helmet": "8.3.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.11.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.5",
    "opossum": "^10.0.0",
    "pg": "^8.23.0",
    "prisma": "^7.9.1",
    "prom-client": "^15.1.3",
    "rate-limit-redis": "^6.0.1",
    "redis": "^6.2.1",
    "reflect-metadata": "^0.2.2",
    "socket.io": "^4.8.3",
    "stellar-sdk": "^13.3.0",
    "swagger-jsdoc": "^6.3.0",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^14.0.2",
    "winston": "^3.17.0",
    "winston-cloudwatch": "^6.3.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.6.0",
    "@stryker-mutator/core": "^10.0.0",
    "@stryker-mutator/vitest-runner": "^10.0.0",
    "@vitest/coverage-v8": "^5.0.0",
    "fast-check": "^4.9.0",
    "fishery": "^2.2.2",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
