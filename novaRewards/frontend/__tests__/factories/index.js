import { faker } from '@faker-js/faker';\n\n// Frontend test factories\n\nexport const createUser = () => ({\n  id: faker.number.int(),\n  email: faker.internet.email(),\n  name: faker.person.fullName(),\n  balance: faker.number.int({ min: 0, max: 10000 }),\n});\n\nexport const createCampaign = () => ({\n  id: faker.number.int(),\n  name: faker.commerce.productName(),\n  pointsPerDollar: faker.number.float({ min: 0.5, max: 2.0 }),\n  active: faker.datatype.boolean(),\n});\n\nexport const createTransaction = () => ({\n  id: faker.string.uuid(),\n  amount: faker.number.float({ min: 1, max: 100 }),\n  pointsEarned: faker.number.int({ min: 1, max: 100 }),\n  status: faker.helpers.arrayElement(['confirmed', 'pending']),\n});
